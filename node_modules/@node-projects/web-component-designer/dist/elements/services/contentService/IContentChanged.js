export {};
/*
export function doesContentChangeAffectDesignItems(change: IContentChanged, designItems: IDesignItem[] | null | undefined): boolean {
  if (!designItems?.length)
    return false;

  if (change.changeType === 'parsed')
    return true;

  return change.designItems.some(item => designItems.includes(item));
}*/ 
//# sourceMappingURL=IContentChanged.js.map