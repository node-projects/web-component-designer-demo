export function didAttributePresenceChange(mutation) {
    if (mutation.type !== 'attributes' || !mutation.attributeName)
        return false;
    const target = mutation.target;
    if (typeof target.getAttribute !== 'function')
        return false;
    const currentValue = target.getAttribute(mutation.attributeName);
    return mutation.oldValue == null || currentValue == null;
}
export function shouldRecreatePropertyLists(mutations) {
    return mutations.some(didAttributePresenceChange);
}
//# sourceMappingURL=PropertyGridMutationHandling.js.map