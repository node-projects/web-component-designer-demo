export function preservesWhitespace(style) {
    return style.whiteSpace === "pre" || style.whiteSpace === "pre-wrap" || style.whiteSpace === "break-spaces";
}
export function normalizeWhitespaceAwareText(text, style) {
    if (preservesWhitespace(style))
        return text.replace(/\r\n?/g, "\n");
    return text.replace(/\s+/g, " ").trim();
}
//# sourceMappingURL=text-whitespace.js.map