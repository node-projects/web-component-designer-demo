export function getCssCanvasFilter(filter) {
    const value = filter?.trim();
    if (!value || value === "none")
        return undefined;
    return value;
}
export function extractBlurRadiusPx(filter) {
    const value = filter?.trim();
    if (!value || value === "none")
        return undefined;
    const matches = [...value.matchAll(/blur\(\s*(-?[\d.]+)px\s*\)/gi)];
    if (matches.length === 0)
        return undefined;
    const radius = matches.reduce((sum, match) => {
        const parsed = parseFloat(match[1]);
        return Number.isFinite(parsed) && parsed > 0 ? sum + parsed : sum;
    }, 0);
    return radius > 0 ? radius : undefined;
}
export function mapMixBlendModeToCanvasComposite(mixBlendMode) {
    switch (mixBlendMode?.trim()) {
        case undefined:
        case "":
        case "normal":
            return undefined;
        case "plus-lighter":
            return "lighter";
        case "multiply":
        case "screen":
        case "overlay":
        case "darken":
        case "lighten":
        case "color-dodge":
        case "color-burn":
        case "hard-light":
        case "soft-light":
        case "difference":
        case "exclusion":
        case "hue":
        case "saturation":
        case "color":
        case "luminosity":
            return mixBlendMode;
        default:
            return undefined;
    }
}
export function getSvgBlendModeStyle(mixBlendMode) {
    const value = mixBlendMode?.trim();
    if (!value || value === "normal")
        return undefined;
    return value;
}
//# sourceMappingURL=filter-effects.js.map