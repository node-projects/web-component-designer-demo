let iframeElementCache = new WeakMap();
export function clearGeometryCaches() {
    iframeElementCache = new WeakMap();
}
function getNodeOwnerDocument(node) {
    if ("nodeType" in node)
        return node.ownerDocument;
    return node.startContainer?.ownerDocument ?? null;
}
function getTopAccessibleDocument(ownerDocument) {
    let topDocument = ownerDocument;
    let currentWindow = ownerDocument.defaultView;
    while (currentWindow?.parent && currentWindow.parent !== currentWindow) {
        try {
            topDocument = currentWindow.parent.document;
            currentWindow = currentWindow.parent;
        }
        catch {
            break;
        }
    }
    return topDocument;
}
function collectSameOriginIframeElements(ownerDocument, results, seenDocuments) {
    if (seenDocuments.has(ownerDocument))
        return;
    seenDocuments.add(ownerDocument);
    for (const iframe of Array.from(ownerDocument.querySelectorAll("iframe"))) {
        results.push(iframe);
        try {
            const childDocument = iframe.contentDocument;
            if (childDocument) {
                collectSameOriginIframeElements(childDocument, results, seenDocuments);
            }
        }
        catch {
            // Ignore cross-origin iframes.
        }
    }
}
export function getBoxQuadsOptions(node, box = "border") {
    const ownerDocument = getNodeOwnerDocument(node);
    if (!ownerDocument)
        return { box };
    const topDocument = getTopAccessibleDocument(ownerDocument);
    let iframes = iframeElementCache.get(topDocument);
    if (!iframes) {
        iframes = [];
        collectSameOriginIframeElements(topDocument, iframes, new Set());
        iframeElementCache.set(topDocument, iframes);
    }
    return iframes.length > 0 ? { box, iframes } : { box };
}
/**
 * Get an element's border-box quad via getBoxQuads.
 * Returns null if the element has zero area.
 */
export function getElementQuad(el, box = "border") {
    if ("getBoxQuads" in el && typeof el.getBoxQuads === "function") {
        try {
            const rawQuads = el.getBoxQuads(getBoxQuadsOptions(el, box));
            if (rawQuads.length > 0) {
                const q = rawQuads[0];
                const quad = [
                    { x: q.p1.x, y: q.p1.y },
                    { x: q.p2.x, y: q.p2.y },
                    { x: q.p3.x, y: q.p3.y },
                    { x: q.p4.x, y: q.p4.y },
                ];
                if (!hasArea(quad))
                    return null;
                return quad;
            }
        }
        catch { /* fall through */ }
    }
    // Fallback (should not happen when polyfill is loaded)
    const r = el.getBoundingClientRect();
    if (r.width === 0 || r.height === 0)
        return null;
    return [
        { x: r.left, y: r.top },
        { x: r.right, y: r.top },
        { x: r.right, y: r.bottom },
        { x: r.left, y: r.bottom },
    ];
}
/**
 * Get all quads for an element (multi-fragment elements may return several).
 */
export function getElementQuads(el, box = "border") {
    if ("getBoxQuads" in el && typeof el.getBoxQuads === "function") {
        try {
            const rawQuads = el.getBoxQuads(getBoxQuadsOptions(el, box));
            return rawQuads.map(domQuadToQuad).filter(hasArea);
        }
        catch { /* fall through */ }
    }
    const r = el.getBoundingClientRect();
    if (r.width === 0 || r.height === 0)
        return [];
    return [[
            { x: r.left, y: r.top },
            { x: r.right, y: r.top },
            { x: r.right, y: r.bottom },
            { x: r.left, y: r.bottom },
        ]];
}
/**
 * Get all quads for any DOM node (Element, Text, or Range) via getBoxQuads.
 * Text nodes and Ranges support getBoxQuads natively.
 */
export function getNodeQuads(node, box = "border") {
    if ("getBoxQuads" in node && typeof node.getBoxQuads === "function") {
        try {
            const rawQuads = node.getBoxQuads(getBoxQuadsOptions(node, box));
            return rawQuads.map(domQuadToQuad).filter(hasArea);
        }
        catch { /* fall through */ }
    }
    // Fallback for Text nodes: use Range.getClientRects()
    if ("nodeType" in node && node.nodeType === 3 /* Node.TEXT_NODE */) {
        const textNode = node;
        const range = (textNode.ownerDocument ?? document).createRange();
        range.selectNodeContents(textNode);
        return rectsToQuads(range.getClientRects());
    }
    // Fallback for Range
    if (!("nodeType" in node)) {
        return rectsToQuads(node.getClientRects());
    }
    const r = node.getBoundingClientRect();
    if (r.width === 0 || r.height === 0)
        return [];
    return [[
            { x: r.left, y: r.top },
            { x: r.right, y: r.top },
            { x: r.right, y: r.bottom },
            { x: r.left, y: r.bottom },
        ]];
}
function rectsToQuads(rects) {
    const quads = [];
    for (const r of rects) {
        if (r.width === 0 && r.height === 0)
            continue;
        quads.push([
            { x: r.left, y: r.top },
            { x: r.right, y: r.top },
            { x: r.right, y: r.bottom },
            { x: r.left, y: r.bottom },
        ]);
    }
    return quads;
}
/** Get the top-left corner of an element's border box via getBoxQuads. */
export function getElementOrigin(el) {
    const q = getElementQuad(el);
    return q ? q[0] : { x: 0, y: 0 };
}
/** Width and height derived from a quad (distance between corners). */
export function quadSize(q) {
    const w = Math.sqrt((q[1].x - q[0].x) ** 2 + (q[1].y - q[0].y) ** 2);
    const h = Math.sqrt((q[3].x - q[0].x) ** 2 + (q[3].y - q[0].y) ** 2);
    return { width: w, height: h };
}
/**
 * Build an SVG CTM that maps from SVG user units to screen coordinates,
 * anchored to the SVG root's getBoxQuads position instead of getScreenCTM.
 * This ensures SVG content aligns with HTML content from getBoxQuads.
 */
export function getSvgScreenCtm(el) {
    try {
        const screenCtm = el.getScreenCTM();
        if (!screenCtm)
            return new DOMMatrix();
        // Get the SVG root element
        const svgRoot = el.ownerSVGElement;
        if (!svgRoot)
            return screenCtm;
        // Compare the SVG root's position from getBoundingClientRect (screen coords)
        // with getBoxQuads (which may come from a polyfill and differ).
        // We use BCR — not rootScreenCtm.e/f — because the root's getScreenCTM()
        // includes viewBox/preserveAspectRatio offsets which are NOT the element's
        // visual top-left corner.
        const bcr = svgRoot.getBoundingClientRect();
        const boxOrigin = getElementOrigin(svgRoot);
        // Only apply adjustment if boxQuads and BCR agree (polyfill is reliable).
        // If they disagree (polyfill bug, e.g. body margin offset), trust screenCtm.
        if (Math.abs(boxOrigin.x - bcr.left) > 1 || Math.abs(boxOrigin.y - bcr.top) > 1) {
            return screenCtm;
        }
        const dx = boxOrigin.x - bcr.left;
        const dy = boxOrigin.y - bcr.top;
        // If there's no difference, skip the adjustment
        if (Math.abs(dx) < 0.01 && Math.abs(dy) < 0.01)
            return screenCtm;
        // Shift the CTM's translation to align with getBoxQuads
        const adjusted = new DOMMatrix([
            screenCtm.a, screenCtm.b,
            screenCtm.c, screenCtm.d,
            screenCtm.e + dx, screenCtm.f + dy,
        ]);
        return adjusted;
    }
    catch {
        return new DOMMatrix();
    }
}
/** Check if a quad has non-zero area. */
function hasArea(q) {
    const ax = q[1].x - q[0].x, ay = q[1].y - q[0].y;
    const bx = q[3].x - q[0].x, by = q[3].y - q[0].y;
    return Math.abs(ax * by - ay * bx) > 0.01;
}
function domQuadToQuad(dq) {
    return [
        { x: dq.p1.x, y: dq.p1.y },
        { x: dq.p2.x, y: dq.p2.y },
        { x: dq.p3.x, y: dq.p3.y },
        { x: dq.p4.x, y: dq.p4.y },
    ];
}
/** Number of line segments used to approximate each superellipse corner. */
const SE_SAMPLES = 8;
export function roundedQuadPath(points, radius, cornerShapes) {
    const N = 4;
    const segments = [];
    // Pre-compute clamped radius per corner (cannot exceed half of shortest adjacent edge)
    const radii = [];
    for (let i = 0; i < N; i++) {
        const prev = points[(i + N - 1) % N];
        const curr = points[i];
        const next = points[(i + 1) % N];
        const toPrevLen = Math.sqrt((prev.x - curr.x) ** 2 + (prev.y - curr.y) ** 2);
        const toNextLen = Math.sqrt((next.x - curr.x) ** 2 + (next.y - curr.y) ** 2);
        radii.push(Math.min(radius, toPrevLen / 2, toNextLen / 2));
    }
    for (let i = 0; i < N; i++) {
        const prev = points[(i + N - 1) % N];
        const curr = points[i];
        const next = points[(i + 1) % N];
        const r = radii[i];
        const K = cornerShapes ? cornerShapes[i] : 1;
        const toPrevLen = Math.sqrt((prev.x - curr.x) ** 2 + (prev.y - curr.y) ** 2);
        const toNextLen = Math.sqrt((next.x - curr.x) ** 2 + (next.y - curr.y) ** 2);
        // Unit vectors from corner toward arc endpoints
        const uHatX = (prev.x - curr.x) / toPrevLen;
        const uHatY = (prev.y - curr.y) / toPrevLen;
        const vHatX = (next.x - curr.x) / toNextLen;
        const vHatY = (next.y - curr.y) / toNextLen;
        // Point where the arc starts (on the edge from prev to curr, near curr)
        const arcStartX = curr.x + uHatX * r;
        const arcStartY = curr.y + uHatY * r;
        if (i === 0) {
            segments.push({ type: "M", x: arcStartX, y: arcStartY });
        }
        else {
            segments.push({ type: "L", x: arcStartX, y: arcStartY });
        }
        if (K === 1 && !cornerShapes) {
            // Default round: quadratic Bézier through the corner (original behavior)
            const arcEndX = curr.x + vHatX * r;
            const arcEndY = curr.y + vHatY * r;
            segments.push({ type: "Q", cx: curr.x, cy: curr.y, x: arcEndX, y: arcEndY });
        }
        else if (K >= 10) {
            // Square: go through the corner vertex (effectively no rounding)
            segments.push({ type: "L", x: curr.x, y: curr.y });
            segments.push({ type: "L", x: curr.x + vHatX * r, y: curr.y + vHatY * r });
        }
        else if (K <= -10) {
            // Notch: two straight lines through the mirror point (R,R) in local coords
            const mirrorX = curr.x + uHatX * r + vHatX * r;
            const mirrorY = curr.y + uHatY * r + vHatY * r;
            segments.push({ type: "L", x: mirrorX, y: mirrorY });
            segments.push({ type: "L", x: curr.x + vHatX * r, y: curr.y + vHatY * r });
        }
        else {
            // General superellipse: sample the curve
            const invK = 1 / Math.abs(K || 0.001);
            const halfPi = Math.PI / 2;
            for (let s = 1; s <= SE_SAMPLES; s++) {
                const t = s / SE_SAMPLES;
                const theta = halfPi * t;
                const sinT = Math.sin(theta);
                const cosT = Math.cos(theta);
                let u, v;
                if (K > 0) {
                    // Convex: curve inscribed in corner (round, squircle, etc.)
                    u = r * (1 - Math.pow(sinT, invK));
                    v = r * (1 - Math.pow(cosT, invK));
                }
                else if (K === 0) {
                    // Bevel: straight line
                    u = r * (1 - t);
                    v = r * t;
                }
                else {
                    // Concave: curve reflected across bevel line (scoop, etc.)
                    u = r * Math.pow(cosT, invK);
                    v = r * Math.pow(sinT, invK);
                }
                const px = curr.x + uHatX * u + vHatX * v;
                const py = curr.y + uHatY * u + vHatY * v;
                segments.push({ type: "L", x: px, y: py });
            }
        }
    }
    return segments;
}
//# sourceMappingURL=geometry.js.map