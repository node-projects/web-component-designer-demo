import { c as createStore } from './index2.js';
import { h as h1, a as h2, d as h3, u as ul, o as ol, b as blockquote, i as img, c as code, e as hr } from './list.plugin.js';
import { D as DEFAULT_PALETTE } from './palette.js';

const DEFAULT_PLUGINS = [h1, h2, h3, ul, ol, blockquote, img, code, hr];
const DEFAULT_TOOLBAR = {
  palette: DEFAULT_PALETTE,
  command: 'native',
  style: {
    list: false,
    align: true,
    fontSize: true,
    backgroundColor: true
  }
};
const DEFAULT_PLACEHOLDERS = ['div', 'p', 'span'];
const DEFAULT_TEXT_PARAGRAPHS = ['h1', 'h2', 'h3', 'div', 'p', 'blockquote'];
const DEFAULT_EXCLUDE_ATTRIBUTES = [
  'placeholder',
  'class',
  'spellcheck',
  'contenteditable',
  'data-gramm',
  'data-gramm_id',
  'data-gramm_editor',
  'data-gr-id',
  'autocomplete'
];
const DEFAULT_PARAGRAPH_IDENTIFIER = 'paragraph_id';
const DEFAULT_ATTRIBUTES = {
  paragraphIdentifier: DEFAULT_PARAGRAPH_IDENTIFIER,
  exclude: [...DEFAULT_EXCLUDE_ATTRIBUTES, DEFAULT_PARAGRAPH_IDENTIFIER]
};
const { state, onChange } = createStore({
  plugins: DEFAULT_PLUGINS,
  toolbar: DEFAULT_TOOLBAR,
  placeholders: DEFAULT_PLACEHOLDERS,
  textParagraphs: DEFAULT_TEXT_PARAGRAPHS,
  menus: undefined,
  attributes: DEFAULT_ATTRIBUTES
});
const configStore = { state, onChange };

export { DEFAULT_PLUGINS as D, DEFAULT_TOOLBAR as a, DEFAULT_PLACEHOLDERS as b, configStore as c, DEFAULT_TEXT_PARAGRAPHS as d, DEFAULT_EXCLUDE_ATTRIBUTES as e, DEFAULT_PARAGRAPH_IDENTIFIER as f };

//# sourceMappingURL=config.store.js.map