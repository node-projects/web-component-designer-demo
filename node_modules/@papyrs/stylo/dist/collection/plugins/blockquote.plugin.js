import { createEmptyElement } from '../utils/create-element.utils.js';
import { transformParagraph } from '../utils/paragraph.utils.js';
export const blockquote = {
  text: 'blockquote',
  icon: 'blockquote',
  createParagraphs: async ({ container, paragraph }) => transformParagraph({
    elements: [createEmptyElement({ nodeName: 'blockquote' })],
    paragraph,
    container
  })
};
