import { createStore } from '@stencil/store';
const { state, onChange, reset } = createStore({
  undo: undefined,
  redo: undefined,
  observe: true
});
export default { state, onChange, reset };
//# sourceMappingURL=undo-redo.store.js.map
