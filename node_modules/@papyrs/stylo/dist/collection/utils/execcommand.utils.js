import { execCommandList } from './execcommand-list.utils.js';
import { execCommandStyle } from './execcommand-style.utils.js';
export function execCommand(selection, action, container) {
  if (!document || !selection) {
    return;
  }
  if (action.cmd === 'style') {
    execCommandStyle(selection, action.detail, container);
  }
  else if (action.cmd === 'list') {
    execCommandList(selection, action.detail, container);
  }
}
